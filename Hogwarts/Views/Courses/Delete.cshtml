@{
  Layout = "_Layout";
}

@model Hogwarts.Models.Course

<h2>Are you sure you want to delete this course from the curriculum?</h2>

@Html.DisplayNameFor(model => model.CourseName): @Html.DisplayFor(model => model.CourseName)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to Course List", "Index")